import palette from '../palette'

export default {
  root: {
    color: palette.icon,
    '&:hover': {
      backgroundColor: '#ffffff',
    },
    marginBottom: 10,
    padding: '2px 15px 2px 15px',
  },
}
