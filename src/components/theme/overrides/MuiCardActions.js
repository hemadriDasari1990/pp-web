import palette from '../palette'

export default {
  root: {
    color: palette.icon,
    '&:hover': {
      backgroundColor: '#ffffff',
    },
    marginBottom: 5,
    padding: '0px !important',
  },
}
